kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: aws-pvc
  labels:
    type: aws-pvc
spec:
  accessModes:
   - ReadWriteOnce
  storageClassName: standard   
  resources:
    requests:
      storage: 3Gi
